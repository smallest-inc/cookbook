# Copy to .env.local (optional — the game now asks each player for their own
# Smallest API key in the browser, so nothing here is required to run it).
#
# PROXY_PORT — override the WS proxy's port. Defaults to 3051.
# PROXY_PORT=3051
