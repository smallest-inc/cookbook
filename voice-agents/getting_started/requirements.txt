# Dependencies for this example
# smallestai>=4.4.7 logs a misleading ERROR at boot:
#   "Startup validation failed — pod will not accept sessions. ValueError: Session not initialized"
# Despite the message, the pod DOES accept sessions and the agent works.
# This is a dry-run validator in smallestai/atoms/crew/server.py that always
# trips on `await session.start()`; safe to ignore. Tracked for an SDK fix.
smallestai>=4.4.6

# Logging
loguru>=0.7.0
