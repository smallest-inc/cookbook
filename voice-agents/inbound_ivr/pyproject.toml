[project]
name = "atoms-inbound-ivr"
version = "0.1.0"
description = "IVR-style agent with intent routing and department transfers"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "smallestai>=4.2.0",
    "python-dotenv",
    "loguru",
]
