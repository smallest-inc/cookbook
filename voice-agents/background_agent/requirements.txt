# Dependencies for this example
#
# Known caveat: _validate_startup in smallestai/atoms/crew/server.py logs a
# spurious 'Startup validation failed — pod will not accept sessions' ERROR
# at boot. The pod still accepts sessions and the multi-node agent works;
# the log is misleading. Tracked for an SDK fix; safe to ignore.
smallestai>=5.3.1

# Logging
loguru>=0.7.0
